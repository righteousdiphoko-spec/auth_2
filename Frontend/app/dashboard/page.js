"use client";
import Link from "next/link";
import Protected from "@/components/Protected";
import { Card } from "@/components/ui";
export default function DashboardPage() { return <Protected>{(user) => <><div className="mb-10"><p className="text-xs font-bold uppercase tracking-[0.2em] text-coral">Your workspace</p><h1 className="mt-3 font-display text-5xl">Welcome, {user.name.split(" ")[0]}.</h1><p className="mt-3 max-w-xl text-ink/60">Your account is ready. Keep your details current and your sign-in secure.</p></div><div className="grid gap-5 md:grid-cols-3"><Card className="md:col-span-2"><p className="text-sm text-ink/50">Signed in as</p><p className="mt-2 text-xl font-semibold">{user.email}</p><div className="mt-6 inline-flex items-center gap-2 rounded-full bg-mist px-3 py-2 text-sm"><span className={`h-2 w-2 rounded-full ${user.emailVerified ? "bg-emerald-600" : "bg-amber-500"}`} />{user.emailVerified ? "Email verified" : "Email verification pending"}</div></Card><Card><p className="font-semibold">Account settings</p><div className="mt-5 grid gap-3 text-sm"><Link className="font-semibold text-coral hover:text-ink" href="/profile">Edit profile</Link><Link className="font-semibold text-coral hover:text-ink" href="/change-password">Change password</Link></div></Card></div></> }</Protected>; }
