"use client";
import Link from "next/link";
import { useState } from "react";
import { AuthLayout } from "@/components/AuthLayout";
import { Alert, Button, Card, Field } from "@/components/ui";
import { request } from "@/lib/api";
import { useRouter } from "next/navigation";
export default function LoginPage() { const router = useRouter(); const [form, setForm] = useState({ email: "", password: "", rememberMe: false }); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const submit = async (e) => { e.preventDefault(); setError(""); setLoading(true); try { await request("/auth/login", form); router.push("/dashboard"); } catch (err) { setError(err.message); } finally { setLoading(false); } }; return <AuthLayout eyebrow="Welcome back" title="Good to see you." footer={<>New here? <Link className="font-bold text-coral" href="/register">Create an account</Link></>}><Card><form className="grid gap-5" onSubmit={submit}><div><h2 className="font-display text-3xl">Sign in</h2><p className="mt-2 text-sm text-ink/60">Use your verified account details.</p></div>{error && <Alert>{error}</Alert>}<Field label="Email" type="email" autoComplete="email" required value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} /><Field label="Password" type="password" autoComplete="current-password" required value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} /><div className="flex items-center justify-between text-sm"><label className="flex items-center gap-2 font-normal"><input type="checkbox" checked={form.rememberMe} onChange={(e) => setForm({ ...form, rememberMe: e.target.checked })} /> Remember me</label><Link className="font-semibold text-coral" href="/forgot-password">Forgot password?</Link></div><Button loading={loading}>Sign in</Button></form></Card></AuthLayout>; }
